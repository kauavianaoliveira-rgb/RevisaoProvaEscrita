function alterarNome() {
    let trocarNome = document.getElementByld('inputNome') value;

    if (trocarNome === "") {
        alert('Digite um Nome')
    } else {
        document.getElementByld()
    }
}